import { notFound } from "next/navigation";
import { AdminEventActions } from "@/components/admin-event-actions";
import { formatCoefficient, formatLira, formatProbability, marketView, totalPool } from "@/domain/market";
import { getEventById } from "@/infrastructure/mock/store";
export default async function AdminEventPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const event = getEventById(id); if (!event) notFound(); const outcomes = marketView(event); return <div><p className="eyebrow">{event.category} · {event.status}</p><h1 className="serif mt-1 max-w-3xl text-3xl font-bold">{event.emoji} {event.title}</h1><p className="mt-2 max-w-2xl text-sm text-[#6f7a72]">{event.description}</p><div className="mt-7 grid gap-6 lg:grid-cols-[1fr_20rem]"><section><div className="mb-3 flex items-center justify-between"><h2 className="serif text-xl font-bold">Стаканы</h2><strong className="text-sm">Всего {formatLira(totalPool(event.outcomes))}</strong></div><div className="card overflow-hidden">{outcomes.map((outcome, index) => <div key={outcome.id} className="grid grid-cols-[2rem_1fr_auto] items-center gap-3 border-b border-[#174b38]/8 p-4 last:border-0"><span className="grid size-7 place-items-center rounded-lg bg-[#174b38]/7 text-xs font-bold">{index + 1}</span><div><strong className="text-sm">{outcome.title}</strong><small className="mt-1 block text-[#7b827c]">старт {formatLira(outcome.seedPool)} · {outcome.bettorsCount} участников</small></div><div className="text-right"><strong>{formatProbability(outcome.probabilityBps)}</strong><small className="block text-[#7b827c]">×{formatCoefficient(outcome.coefficient)} · {formatLira(outcome.pool)}</small></div></div>)}</div><p className="mt-3 text-xs leading-relaxed text-[#798179]">После первой принятой ставки состав исходов и стартовую ликвидность менять нельзя. Исправления названий должны попадать в аудит.</p></section><aside className="card h-fit p-5"><h2 className="serif text-xl font-bold">Результат события</h2><p className="mb-4 mt-1 text-xs leading-relaxed text-[#7a827b]">Победивший стакан получит общий пул пропорционально позициям. Комиссии нет.</p><AdminEventActions event={event} /></aside></div></div>; }
