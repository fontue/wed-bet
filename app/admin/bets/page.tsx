import { formatCoefficient, formatLira } from "@/domain/market";
import { listEvents, listUsers, store } from "@/infrastructure/mock/store";
export default function AdminBetsPage() { const users = listUsers(); const events = listEvents(); const bets = structuredClone(store.bets).sort((a,b) => b.createdAt.localeCompare(a.createdAt)); return <div><p className="eyebrow">Tutti i biglietti</p><h1 className="serif mt-1 text-3xl font-bold">Все ставки</h1><div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[46rem] border-collapse text-left text-sm"><thead className="bg-[#174b38] text-[.65rem] uppercase tracking-wider text-white/65"><tr><th className="p-3">Гость</th><th className="p-3">Событие</th><th className="p-3">Исход</th><th className="p-3">Сумма</th><th className="p-3">Коэф. при входе</th><th className="p-3">Статус</th></tr></thead><tbody>{bets.map((bet) => { const user = users.find((item) => item.id === bet.userId); const event = events.find((item) => item.id === bet.eventId); const outcome = event?.outcomes.find((item) => item.id === bet.outcomeId); return <tr key={bet.id} className="border-b border-[#174b38]/8"><td className="p-3 font-bold">{user?.displayName ?? "Архивный гость"}</td><td className="max-w-64 truncate p-3">{event?.title ?? "Архивное событие"}</td><td className="p-3">{outcome?.title ?? "—"}</td><td className="p-3 font-bold">{formatLira(bet.amount)}</td><td className="p-3">×{formatCoefficient(bet.projectedCoefficient)}</td><td className="p-3"><span className="rounded-full bg-[#174b38]/8 px-2 py-1 text-[.6rem] font-extrabold">{bet.status}</span></td></tr>; })}</tbody></table></div></div>; }
