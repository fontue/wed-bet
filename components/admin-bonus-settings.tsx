"use client";
import { useState } from "react";
import type { BonusDefinition } from "@/domain/models";
export function AdminBonusSettings({ initialDefinitions, initialInterval }: { initialDefinitions: BonusDefinition[]; initialInterval: number }) { const [definitions, setDefinitions] = useState(initialDefinitions); const [interval, setInterval] = useState(initialInterval); const [message, setMessage] = useState(""); const total = definitions.filter((item) => item.active).reduce((sum, item) => sum + item.weight, 0); async function save() { const response = await fetch("/api/admin/bonuses", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify({ intervalMinutes: interval, definitions: definitions.map(({ id, weight, active }) => ({ id, weight, active })) }) }); setMessage(response.ok ? "Настройки сохранены" : "Не удалось сохранить"); } return <div className="grid gap-6 lg:grid-cols-[1fr_20rem]"><section className="card overflow-hidden"><div className="grid grid-cols-[1fr_6rem_6rem] bg-[#174b38] px-4 py-3 text-[.65rem] font-extrabold uppercase tracking-wider text-white/65"><span>Награда</span><span>Вес</span><span>Активна</span></div>{definitions.map((item) => <div key={item.id} className="grid grid-cols-[1fr_6rem_6rem] items-center gap-2 border-b border-[#174b38]/8 p-4"><div className="flex items-center gap-3"><span className="size-8 rounded-full" style={{ background: item.color }} /><div><strong className="text-sm">{item.label}</strong><small className="block text-[#7d847e]">{item.kind} · {total ? Math.round(item.weight / total * 100) : 0}%</small></div></div><input className="input !min-h-10 !p-2" type="number" min={0} value={item.weight} onChange={(e) => setDefinitions((items) => items.map((definition) => definition.id === item.id ? { ...definition, weight: Number(e.target.value) } : definition))} /><input className="size-5 accent-[#174b38]" type="checkbox" checked={item.active} onChange={(e) => setDefinitions((items) => items.map((definition) => definition.id === item.id ? { ...definition, active: e.target.checked } : definition))} /></div>)}</section><aside className="card h-fit p-5"><label className="text-xs font-extrabold uppercase tracking-wider">Интервал вращения, минут</label><input className="input mt-2" type="number" min={1} value={interval} onChange={(e) => setInterval(Number(e.target.value))} /><p className="mt-3 text-xs leading-relaxed text-[#788078]">Вероятность рассчитывается из веса активного сектора относительно суммы весов.</p><button className="btn-primary mt-5 w-full" onClick={() => void save()}>Сохранить</button>{message && <p className="mt-3 text-center text-xs font-bold">{message}</p>}</aside></div>; }
